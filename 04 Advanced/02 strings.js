// Template literals in javascript

var name = `hafiz`;
var fullName = `${name} mp`;

console.log(fullName);
console.log(fullName.charAt(0));
console.log(fullName.endsWith("?"));
console.log(fullName.includes("hafiz"));
console.log(fullName.toUpperCase());